<Window
    x:Class="Washmachine.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="Washmachine - Loader Builder"
    Width="920"
    Height="760">

    <Grid x:Name="Root">
        <controls:TabView x:Name="tabControl">
            <controls:TabViewItem Header="Main">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16" Margin="16,12,16,20">
                        <StackPanel Spacing="6">
                            <TextBlock Text="Shellcode" FontWeight="SemiBold" />
                            <Border
                                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="8">
                                <controls:Pivot x:Name="shellcodeType">
                                    <controls:PivotItem Header="File">
                                        <StackPanel Spacing="8" Margin="8">
                                            <TextBlock Text="Shellcode file (typically .bin):" />
                                            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                                                <TextBox x:Name="shellcodeFile" Grid.Column="0" />
                                                <Button x:Name="button1" Grid.Column="1" Content="Browse" Click="button1_Click" MinWidth="86" />
                                                <Button x:Name="WebPayloadGenerator" Grid.Column="2" Content="Web Payload" Click="WebPayloadGenerator_Click" MinWidth="110" />
                                            </Grid>
                                        </StackPanel>
                                    </controls:PivotItem>
                                    <controls:PivotItem Header="Raw">
                                        <StackPanel Spacing="8" Margin="8">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <TextBlock Text="Paste a raw shellcode:" />
                                                <HyperlinkButton x:Name="RAWShellcodeInfo" Content="Format?" Click="RAWShellcodeInfo_Click" Padding="0" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                                <TextBox
                                                    x:Name="shellcodeRAW"
                                                    Grid.Column="0"
                                                    AcceptsReturn="True"
                                                    TextWrapping="NoWrap"
                                                    HorizontalScrollBarVisibility="Auto"
                                                    VerticalScrollBarVisibility="Auto"
                                                    FontFamily="Consolas"
                                                    Height="100" />
                                                <Button x:Name="button2" Grid.Column="1" Content="Paste" Click="button2_Click" VerticalAlignment="Bottom" MinWidth="86" />
                                            </Grid>
                                        </StackPanel>
                                    </controls:PivotItem>
                                    <controls:PivotItem Header="URL">
                                        <StackPanel Spacing="8" Margin="8">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <TextBlock Text="From a URL:" />
                                                <HyperlinkButton Content="Guide" Click="RAWShellcodeInfo_Click" Padding="0" />
                                            </StackPanel>
                                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                                <TextBox x:Name="shellcodeURL" Grid.Column="0" />
                                                <Button x:Name="button3" Grid.Column="1" Content="Paste" Click="button3_Click" MinWidth="86" />
                                            </Grid>
                                        </StackPanel>
                                    </controls:PivotItem>
                                    <controls:PivotItem Header="Generic">
                                        <StackPanel Spacing="8" Margin="8">
                                            <TextBlock Text="Choose a generic shellcode (for testing purposes):" />
                                            <ComboBox x:Name="genericShellcodeComboBox" Width="360" />
                                        </StackPanel>
                                    </controls:PivotItem>
                                </controls:Pivot>
                            </Border>
                        </StackPanel>

                        <StackPanel Spacing="6">
                            <TextBlock Text="Payload Encoding" FontWeight="SemiBold" />
                            <Border
                                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="8">
                                <Grid ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="12" RowDefinitions="Auto">
                                    <TextBlock Text="Encoder:" VerticalAlignment="Center" />
                                    <ComboBox x:Name="bin2hexEncoder" Grid.Column="1" MinWidth="220" />
                                    <TextBlock Text="Envelope:" Grid.Column="2" VerticalAlignment="Center" />
                                    <ComboBox x:Name="bin2hexEnvelope" Grid.Column="3" MinWidth="220" />
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel Spacing="6">
                            <TextBlock Text="Template" FontWeight="SemiBold" />
                            <Border
                                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="8">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                    <ComboBox x:Name="templateComboBox" Grid.Column="0" SelectionChanged="templateComboBox_SelectedIndexChanged" />
                                    <Button x:Name="button4" Grid.Column="1" Content="Config Template" Click="button4_Click" MinWidth="130" />
                                </Grid>
                            </Border>
                        </StackPanel>

                        <StackPanel HorizontalAlignment="Center">
                            <Button x:Name="submitButton" Content="Compile" Click="submitButton_Click" Width="130" />
                        </StackPanel>

                        <StackPanel Spacing="6">
                            <TextBlock Text="Logs" FontWeight="SemiBold" />
                            <Border
                                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="8">
                                <RichEditBox
                                    x:Name="debugBox"
                                    Height="150"
                                    IsReadOnly="True"
                                    TextWrapping="NoWrap"
                                    HorizontalScrollBarVisibility="Auto"
                                    VerticalScrollBarVisibility="Auto"
                                    FontFamily="Consolas" />
                            </Border>
                        </StackPanel>

                        <TextBlock
                            x:Name="textBox4"
                            TextWrapping="Wrap"
                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                            Text="This tool is provided strictly for educational and research purposes. The author makes no representations or warranties of any kind regarding its accuracy, reliability, or suitability for any purpose. By using this tool, you agree that you are solely responsible for ensuring your actions comply with all applicable laws and regulations. The author explicitly disclaims any liability for misuse, damage, or unlawful activity carried out with or derived from this software. Use it at your own risk." />
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

            <controls:TabViewItem Header="Packing">
                <Grid>
                    <TextBlock Text="Packing workflow coming soon." HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" />
                </Grid>
            </controls:TabViewItem>

            <controls:TabViewItem Header="Backdooring">
                <Grid>
                    <TextBlock Text="Backdooring workflow coming soon." HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" />
                </Grid>
            </controls:TabViewItem>
        </controls:TabView>
    </Grid>
</Window>
